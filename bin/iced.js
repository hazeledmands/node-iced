// Generated by CoffeeScript 1.6.3
var args, description, knownOpts, nopt, shortHands, version;

nopt = require('nopt');

knownOpts = {
  version: Boolean,
  help: Boolean
};

shortHands = {
  v: ['--version'],
  h: ['--help']
};

args = nopt(knownOpts, shortHands, process.argv);

if (args.version) {
  version = require('../package.json').version;
  console.log(version);
  process.exit(0);
}

if (args.help) {
  description = require('../package.json').description;
  console.log(description);
  process.exit(0);
}
